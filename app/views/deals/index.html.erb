<div class="container">
  <h1>Negocios subidos en la última carga</h1>
  <ul>
    <% @deals.each do |d| %>
      <li>
        <%= d.ecid  %> -
        <%= d.legal_state.name  %>
      </li>
    <% end %>
  </ul>
</div>
